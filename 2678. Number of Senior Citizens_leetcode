class Solution:
    def countSeniors(self, details: List[str]) -> int:
        cnt = 0
        for ticket in details:
            if int(ticket[-4:-2]) > 60:
                cnt += 1
        
        return cnt

